fn main() {
    // Exam score configuration
    let score = 35;
    let passing_grade = 50;

    // Check if the student passed based on the threshold
    if score >= passing_grade {
        println!("Passed the exam.");
    } else {
        println!("Failed. Study harder.");
    }
}